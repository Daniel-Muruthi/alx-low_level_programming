#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
 * _abs - computes the absolute value of an integer
 *
 *
 *
 * Return: 0(success)
 *
 */
int _abs(int r)
{
	r = abs(r);
	_putchar(r);
	return (0);
}
