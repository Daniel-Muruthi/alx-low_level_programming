#include "main.h"
/**
 * _abs - Entry point
 * Description - computes the absolute value of an integer
 * @int - Value to be absoluted
 * Return: Absolute value
 */
int _abs(int i)
{
	int j;

	j = abs(i);
	return (j);
}
