#include "main.h"
#include <ctype.h>
/**
 * _abs - Entry point
 * Description - computes the absolute value of an integer
 * @i - Value to be absoluted
 * Return: Absolute value
 */
int _abs(int i)
{
	int j;

	j = abs(i);
	return (j);
}
