#include "main.h"
#include <stdio.h>
/**
 * add - Entry point
 * Description - Adds two integers and returns the results
 * @i: First integer
 * @j: Second integer
 * Return: Returns the summation of the integers
 */
int add(int i, int j)
{
	int total;

	total = i + j;
	return (total);
}
