#include <string.h>
#include <stdio.h>
/**
 * puts2 - Entry point
 * Description - Prints all characters of a string followed by a new line
 * @str: string printed
 */
void puts2(char *str)
{
	puts(str);
}
