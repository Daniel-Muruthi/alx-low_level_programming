#include <string.h>
#include <stdlib.h>
/**
 * _strlen - Entry point
 * Description - Returns the length of a string
 * @s: string element
 */
int _strlen(char *s)
{
	int i;

	i = strlen(*s);
	return (i);
}
