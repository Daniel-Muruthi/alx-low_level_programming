#include <stdio.h>
#include <string.h>
/**
 * _strcpy - Entry Point
 * Destination - Copies string from source to destination
 * @dest: Destination
 * @src: Source
 * Return: string copied
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
